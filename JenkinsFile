pipeline {
    agent any
    stages {
        stage ('Build Backend') {
            steps {
                script {
                    sh 'mvn clean package -DskipTests=true'
                }
            }
        }
    }
}